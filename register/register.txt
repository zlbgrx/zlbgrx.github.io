	注册信息说明：
	1).输入框捕获焦点是，提示对应栏目的填写要求，所有输入信息均在输入框失去焦点后进行验证
	2).用户名暂时只能以手机号码注册，如果格式不符合在输入框下方提示验证未通过；
	3).密码可使用6-20位字符，可使用字母数字或字符的组合，验证不通过时提示相应信息；
	4).比对密码与第二次输入的密码是否一致，不一致提示先关信息；
	5).验证码须与右侧随机生成的验证码一致；
	6).手机验证码不做验证；
	7).点击注册前必须同意用户协议；
	8).点击注册后进行错误追踪，看是否有项目未填，并作出相应的提示；
	9).注册通过后，将用户名和密码存入cookie。
	








if(usn == 0) { //输入框为空时，提示请输入手机号
			$(this).parent('p').siblings('.error').css('display', 'block').html("请输入手机号");
			return false;
		} else { //非空情况下
			if(reg.phone.test(usn)) { //如果手机号格式正确，隐藏提示项
				$(this).parent('p').siblings('.error').css('display', 'none');
				return true;
			} else { //手机号格式有误，提示账号暂时只开放手机号码注册
				$(this).parent('p').siblings('.error').css('display', 'block').html("账号暂时只开放手机号码注册");
				return false;
			}
		}